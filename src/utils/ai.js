export const chatWithPersonality = async (message, personality) => {
  // This is a placeholder function.
  // In a real app, you would call your backend API to get AI responses.
  // For now, just return a canned response.
  return `This is a simulated response from ${personality.name} to your message: "${message}"`;
};
